# WAF 资源高占用处置参考

当巡检文档显示 CPU、内存或磁盘使用率偏高时，建议优先：

1. 结合当前资源快照确认是否为持续高负载，而非瞬时波动。
2. 对容器化部署优先查看 `docker stats`，确认是否由单个关键容器占用过高。
3. 对主机资源优先查看 `free -h`、磁盘占用明细与系统负载，确认是否存在资源泄漏或空间持续增长。
4. 对磁盘高占用场景优先核查日志目录、备份目录、临时文件与历史产物，必要时评估清理或扩容。

文档直审模式下，若缺少运行日志与历史趋势证据，应使用保守措辞，例如“建议进一步结合运行日志核查”。
